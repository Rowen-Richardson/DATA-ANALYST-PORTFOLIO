SELECT *
FROM employee_demographics;

SELECT first_name, 
last_name, 
birth_date,
age,
age + (10 * 10 )
#this is PEMDAS ALSO BODMAS for short
FROM employee_demographics;

SELECT distinct first_name, gender
FROM employee_demographics;

# This is a comment same as python it works the same way basicallly 